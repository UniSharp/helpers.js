{
  "name": "@unisharp/helpers.js",
  "version": "2.1.0",
  "description": "JavaScript useful helpers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": {
    "name": "UniSharp Ltd.",
    "email": "unisharp-service@unisharp.com"
  },
  "scripts": {
    "lint": "eslint --ext .ts .",
    "lint:fix": "yarn lint --fix",
    "test": "yarn lint && jest",
    "compile": "tsc",
    "clean": "rm -rf dist",
    "build": "yarn test && yarn clean && yarn compile"
  },
  "license": "MIT",
  "files": [
    "dist",
    "nuxt"
  ],
  "devDependencies": {
    "@nuxtjs/eslint-config-typescript": "latest",
    "@types/jest": "latest",
    "coveralls": "latest",
    "eslint": "latest",
    "jest": "latest",
    "ts-jest": "latest",
    "typescript": "~4.2"
  },
  "dependencies": {
    "@unisharp/babel-plugin": "^0.3.7"
  }
}
